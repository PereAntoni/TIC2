@startuml
!theme toy
start
    :llegir A;
    :llegir B;
    if (A major que B) then (yes)
        :proces;
    else (no)
        :X=A;
        :A=E;
        :B=X;
    endif
    :print(A,B);
stop
@enduml